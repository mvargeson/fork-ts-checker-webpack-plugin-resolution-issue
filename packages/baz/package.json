{
  "name": "@namespace/baz",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "webpack",
    "build:fixed": "webpack --config webpack.config.fixed.ts",
    "clean": "rm -rf dist *.tsbuildinfo",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@namespace/bar": "0.1.0"
  }
}
